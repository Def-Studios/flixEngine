import pygame
from pygame.locals import *
from playsound import playsound
import sys
# so we can read paths and gameinfo
import json
class FxTools:
    def playSound(path):
        playsound(path)
        #very usefull :flushed:
